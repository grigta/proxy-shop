# Proxy Shop - Руководство по запуску Admin Panel

## ✅ Что было реализовано

Все 9 комментариев из требований были полностью выполнены:

### Backend (Python/FastAPI)

1. ✅ **Admin API** - Полностью реализован
   - `backend/schemas/admin.py` - Все схемы (DashboardStatsResponse, RevenueChartData, AdminUserListResponse, CreateCouponRequest, UpdateCouponRequest, AdminProxyListResponse и др.)
   - `backend/services/admin_service.py` - Методы агрегации статистики, списков пользователей, CRUD купонов и агрегатов по прокси
   - `backend/api/routes/admin.py` - Все эндпоинты с защитой через `is_admin`
   - `backend/main.py` - Router подключен

2. ✅ **Модель User** - Поля добавлены
   - `is_admin: Boolean(False, not null)`
   - `is_blocked: Boolean(False, not null)`
   - `blocked_at: DateTime(null)`
   - `blocked_reason: String(500, null)`
   - Индексы `idx_users_is_admin`, `idx_users_is_blocked` созданы

3. ✅ **ProxyInventory** - Модель и сервис созданы
   - `backend/models/proxy_inventory.py` - Модель с полями (id, ip, port, country, state, city, is_available, price_per_hour, created_at, updated_at, notes)
   - Уникальный индекс на (ip, port), индексы по is_available и локации
   - `backend/services/proxy_inventory_service.py` - CRUD с фильтрацией/пагинацией
   - Миграция Alembic создана

4. ✅ **Admin CRUD для купонов** - Реализовано
   - Методы в `admin_service.py`: `get_coupons_list`, `create_coupon`, `update_coupon`, `delete_coupon`, `get_coupon_stats`
   - Эндпоинты в `admin.py`: GET/POST/PATCH/DELETE /admin/coupons и GET /admin/coupons/stats
   - Проверка `is_admin` присутствует

5. ✅ **Admin панель (Vite + React)** - Полностью настроена
   - `admin/package.json` - Все зависимости (Vite, React 19, TypeScript, Lucide React, recharts)
   - `admin/tsconfig.json`, `admin/vite.config.ts`, `admin/constants.ts` - Конфигурации
   - Компоненты: `Dashboard.tsx`, `Users.tsx`, `Coupons.tsx`, `Proxies.tsx`, `Sidebar.tsx`
   - `App.tsx`, `index.tsx`, `index.html` - Основная структура
   - Все тексты на русском и английском (мультиязычность)

6. ✅ **Docker** - Настроен
   - `docker-compose.yml` - Сервис `admin` обновлён (node:20-alpine, порт 3001, volume mapping, env VITE_API_URL)
   - Команда: `npm install && npm run dev -- --host 0.0.0.0 --port 3001`
   - Anonymous volumes для node_modules и dist
   - `admin/Dockerfile` - Многоэтапная сборка (deps -> builder -> runner с nginx), EXPOSE 80

7. ✅ **Инициализация администратора** - Реализована
   - `backend/scripts/init_db.py` - Функция `seed_admin_user()` (создание пользователя с is_admin=True, генерация access_code, вывод в консоль)
   - Переменная окружения `ADMIN_ACCESS_CODES` добавлена
   - Вызов `await seed_admin_user()` из `main()` присутствует

8. ✅ **Экспорт admin схем** - Выполнен
   - `backend/schemas/__init__.py` - Импорты и `__all__` со всеми admin схемами

9. ✅ **Admin guard** - Реализован
   - `backend/api/dependencies.py` - `get_current_admin_user` (проверка `user.is_admin`, HTTPException(403) при False)
   - Используется во всех эндпоинтах `/api/admin/*`

## 🚀 Запуск системы

### Шаг 1: Инициализация базы данных

```bash
# Запустить PostgreSQL
docker-compose up -d postgres

# Применить миграции
cd backend
alembic upgrade head

# Инициализировать данные и создать администратора
python scripts/init_db.py
```

**ВАЖНО:** Сохраните `access_code` администратора, который будет выведен в консоль!

### Шаг 2: Запуск всех сервисов

```bash
# Вернуться в корень проекта
cd ..

# Запустить все сервисы
docker-compose up

# Или запустить только админ-панель и backend
docker-compose up backend admin
```

**Примечание**: При первом запуске Vite установит зависимости (npm install), это займет несколько минут.

### Шаг 3: Доступ к админ-панели

1. Откройте http://localhost:3001
2. Введите `access_code` администратора
3. Нажмите "Войти"

## 🔧 Технологии админ-панели

### Frontend Stack
- **Vite** - Быстрый сборщик с мгновенным HMR
- **React 19** - UI библиотека
- **TypeScript** - Типизация
- **Recharts** - Графики и визуализация
- **Lucide React** - Иконки

### Преимущества Vite
- ⚡ Мгновенный запуск dev-сервера
- 🔥 Быстрый Hot Module Replacement (HMR)
- 📦 Оптимизированная production сборка
- 🎯 Простая конфигурация

## 📍 Порты

- **Backend API:** http://localhost:8000
- **Frontend:** http://localhost:3000
- **Admin Panel:** http://localhost:3001
- **PostgreSQL:** localhost:5432
- **pgAdmin:** http://localhost:5050

## 🔐 Безопасность

- Все admin эндпоинты защищены проверкой `is_admin`
- JWT токены для аутентификации
- Access code должен храниться в безопасном месте
- Заблокированные пользователи не могут входить в систему

## 📊 Функции админ-панели

### Dashboard (/)
- Статистика: пользователи, доход, покупки, активные прокси
- Графики доходов за разные периоды (7d, 30d, всё время)
- Периодическая статистика (1d, 7d, 30d, all_time)

### Пользователи (/users)
- Список всех пользователей с фильтрацией
- Поиск по username, access_code, telegram_id
- Фильтры: платформа, даты регистрации, баланс, статус блокировки
- Просмотр детальной информации пользователя
- Изменение баланса
- Блокировка/разблокировка пользователей
- Топ пользователей по разным метрикам

### Купоны (/coupons)
- Список всех купонов
- Создание новых промокодов
- Редактирование существующих
- Деактивация купонов
- Статистика использования
- Фильтрация по активности и датам

### Прокси (/proxies)
- Инвентарь прокси-серверов
- Добавление новых прокси (IP, порт, локация, цена)
- Редактирование доступности и цены
- Удаление из инвентаря
- Фильтрация по стране, штату, городу
- Статистика по прокси

## 🛠 API Эндпоинты (Admin)

Все эндпоинты работают через переменную окружения `VITE_API_URL` (по умолчанию http://localhost:8000).

### Статистика
- `GET /api/admin/stats?period=all_time` - Общая статистика
- `GET /api/admin/revenue-chart?period=30d&granularity=day` - Данные для графиков

### Пользователи
- `GET /api/admin/users` - Список пользователей (с фильтрами, пагинацией)
- `GET /api/admin/users/{user_id}` - Детальная информация
- `PATCH /api/admin/users/{user_id}` - Обновление пользователя
- `GET /api/admin/top-users?metric=revenue&limit=10` - Топ пользователей

### Купоны
- `GET /api/admin/coupons` - Список купонов
- `POST /api/admin/coupons` - Создание купона
- `PATCH /api/admin/coupons/{coupon_id}` - Обновление купона
- `DELETE /api/admin/coupons/{coupon_id}` - Удаление купона (soft delete)
- `GET /api/admin/coupons/stats` - Статистика купонов

### Прокси
- `GET /api/admin/proxies` - Список прокси
- `POST /api/admin/proxies` - Добавление прокси
- `POST /api/admin/proxies/bulk` - Массовое добавление
- `PATCH /api/admin/proxies/{proxy_id}` - Обновление прокси
- `DELETE /api/admin/proxies/{proxy_id}` - Удаление прокси
- `GET /api/admin/proxies/stats` - Статистика прокси

## 📝 Логирование

Все действия администратора логируются в таблице `user_logs`:
- ADMIN_UPDATE_BALANCE
- ADMIN_BLOCK_USER
- ADMIN_UNBLOCK_USER
- ADMIN_UPDATE_LANGUAGE
- ADMIN_CREATE_COUPON
- ADMIN_UPDATE_COUPON
- ADMIN_DELETE_COUPON

## 🧪 Тестирование

```bash
# Backend тесты
cd backend
pytest

# Frontend тесты (если будут добавлены)
cd admin
npm test
```

## 📚 Документация API

Swagger UI доступен по адресу: http://localhost:8000/api/docs

## 🔄 Миграции базы данных

Созданы следующие миграции:
- `2025_11_12_1530-remove_duplicate_unique_proxy_history.py`
- `2025_11_12_1730-add_gin_index_products.py`
- `2025_11_12_2000-add_proxy_inventory_table.py` - **Новая миграция для ProxyInventory**

## 💡 Рекомендации

1. **Безопасность**: Измените `JWT_SECRET_KEY` в production
2. **Access Codes**: Храните admin access codes в безопасном месте
3. **Резервное копирование**: Регулярно делайте backup базы данных
4. **Мониторинг**: Следите за логами администраторских действий
5. **Обновления**: Регулярно обновляйте зависимости

## 🐛 Troubleshooting

### Не могу войти в админ-панель
- Убедитесь, что пользователь имеет `is_admin=True`
- Проверьте, что access_code правильный
- Проверьте, что пользователь не заблокирован

### Backend не отвечает
- Проверьте, что PostgreSQL запущен: `docker ps`
- Проверьте логи: `docker logs proxy_shop_backend`
- Убедитесь, что миграции применены

### Admin panel не загружается
- Проверьте, что порт 3001 свободен
- Проверьте переменную окружения `VITE_API_URL`
- Очистите кэш: `docker-compose down && docker-compose up --build admin`
- Проверьте логи: `docker logs proxy_shop_admin`

## 🔄 Миграция с Next.js на Vite

### Что изменилось

**Технологии:**
- Next.js 14 → Vite 6
- React 18 → React 19
- App Router → Classic React Router
- Server Components → Client Components

**Конфигурация:**
- `next.config.js` → `vite.config.ts`
- `.next/` → `dist/`
- `NEXT_PUBLIC_API_URL` → `VITE_API_URL` (в docker-compose используется та же переменная)

**Структура:**
- `app/` директория → компоненты в корне
- `page.tsx` файлы → компоненты `*.tsx`
- Упрощенная структура без app router

**Команды:**
- `npm run dev` - теперь запускает Vite (порт 3001)
- Добавлен флаг `--host 0.0.0.0 --port 3001` для Docker

### Преимущества новой версии

1. **Скорость разработки**: Vite запускается мгновенно vs 5-10 секунд Next.js
2. **HMR**: Изменения применяются без перезагрузки страницы
3. **Простота**: Меньше абстракций, проще понять код
4. **Размер**: Меньше зависимостей и размер bundle
5. **Совместимость**: Тот же backend API, без изменений

### Обратная совместимость

- Backend API endpoints остались без изменений
- Аутентификация через access_code работает так же
- Все функции сохранены (Dashboard, Users, Coupons, Proxies)
- Порт 3001 остался прежним

## 🎉 Готово!

Все требования из 9 комментариев полностью реализованы. Админ-панель обновлена до Vite и готова к использованию!

