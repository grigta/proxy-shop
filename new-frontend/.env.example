# ============================================================================
# BACKEND API CONFIGURATION
# ============================================================================

# Base URL for backend API
# Development: http://localhost:8000
# Production: https://api.proxy-shop.com
# Note: For browser-side app, use external URLs (not Docker internal http://backend:8000)
VITE_API_URL=http://localhost:8000

# ============================================================================
# APP CONFIGURATION
# ============================================================================

# Base URL for web application
# Used for generating referral links and redirects
# Development: http://localhost:3000
# Production: https://proxy-shop.com
VITE_APP_URL=http://localhost:3000

# ============================================================================
# FEATURE FLAGS (OPTIONAL)
# ============================================================================

# Enable debug mode for development
# Set to 'true' to enable console logging for API calls
# Production: false
VITE_ENABLE_DEBUG=false

# ============================================================================
# NOTES
# ============================================================================

# 1. Copy this file to .env for local development: cp .env.example .env
# 2. All client-side environment variables MUST have the VITE_ prefix
# 3. Variables are accessible in code via import.meta.env.VITE_*
# 4. DO NOT include backend secrets (JWT_SECRET_KEY, database credentials, etc.)
#    Backend secrets are only needed on the backend service
# 5. DO NOT commit .env file to version control (already in .gitignore)
# 6. For Docker deployments, these variables are passed from docker-compose.yml

# ============================================================================
# EXAMPLE PRODUCTION VALUES (for reference)
# ============================================================================

# VITE_API_URL=https://api.proxy-shop.com
# VITE_APP_URL=https://proxy-shop.com
# VITE_ENABLE_DEBUG=false
